---
description: 分层式系统架构图 draw.io 样式专规；蓝阶 L0～L4、线型、画布分栏、块/备注/底栏、v3 外框。设计规划与方案对齐见 skillsOfYao work/workflow/layered-architecture-drawio
globs:
  - "**/*.drawio"
  - "**/*架构*.drawio"
  - "**/*architecture*.drawio"
  - "**/*总体*方案*.md"
  - "**/*技术方案*.md"
alwaysApply: false
---

# 分层式系统架构图 · 样式与版式（Rule）

**配对技能**：`work/workflow/layered-architecture-drawio/SKILL.md`（层次抽象、与方案文档对齐、出图前检查、主链叙事）。本文件管**色线布局参数**；规划体不在此重复。

## 适用范围

- **适用**：**自上而下或分栏的层次化** 系统架构总览（能力层/资产/外部等纵向或左中右分栏结构），与 **技术方案** 配图同叙事。
- **不适用**：**多泳道、时序/交互步进、流程泳道** 类图；此类用通用 `cursor-drawio` 或单独规范，**不由本规则约束**。

## 设计原则

- **语义用色、少色种**：一条 **蓝系色阶** 表达「抽象 → 可执行 → 外联/出口」，避免多色分区增加解读成本。**存量/非目标** 单独 **蓝灰 + 虚线**，表示对比、非终态，不抢主叙事。
- **关系用线型**：**虚线** = 约束、资产、方法论类关系；**粗实线** = 跨组主链（先进单源能力、再进外联）；组内 **细实线**。读色知层次、读线知关系。
- **版式先于装饰**：先分清 **主节点 / 子节点 / 备注 / 图形说明** 四类，再定尺寸与对齐，最后叠备注和底栏图说；避免主图区被说明文字撑乱或图说漂在中间。
- **与产品表述一致**：图中「单源、中部能力面」等须与方案文档一致，避免图文两套叙事。

## 色（主色蓝阶 + 例外）

| 阶 | 语义 | 填充 | 描边 |
| --- | --- | --- | --- |
| L0 | 大分组/宿主栏最浅衬底 | `#F5F9FF` | `#1976D2` 或 `#64B5F6` |
| L1 | 区划/资产层 | `#E3F2FD` | `#1565C0` |
| L2 | 子模块/卡片 | `#BBDEFB` | `#1565C0` |
| L3 | 强调、Tool/CLI 核心 | `#90CAF9`～`#64B5F6` | `#0D47A1` 或 `#01579B` |
| L4 | 外部/观测区划 | 区划底 `#B3E5FC`，内块可 `#E1F5FE` | `#0288D1` |
| 存量/非目标 | 对比带 | `#E8ECEF` | `#78909C`，`dashed=1` |
| 图例底 | 中性条带 | `#ECEFF1` | `#78909C`，`dashed=1` |

禁止默认使用大面积红/彩虹分区（除非文宣另有要求）。

## 字

- 标题：`#0D47A1`
- 正文/框内：`#0D1B2A` 或 `#263238`
- 小字：`#455A64`
- 底栏图形说明：约 `fontSize=8`，`fontColor=#455A64`

## 块与备注

- 同级子块 **等高/等宽推齐**；尺寸由 **最长文案** 定最小高宽。
- 禁止子块与备注拼成一条 **假横排** 造成大块留白。
- 备注：`shape=note`，浅黄底琥珀边，叠在被说明对象 **右上**；`fontSize=7～8`。
- 图形说明：**整页最下** 独立一条/带；可与色阶、图例同带分行；**不占主图中部**；不用长 Note 代替底栏图说。

## 线

- 正交，`rounded=0`；`orthogonalEdgeStyle`。
- 并列块间距约 **16～24**；横向连边用 `exitX`/`entryX` 与 **0.5**，避免贴边。
- 虚线 + `#1565C0`/`#1976D2`：约束、资产下发。
- 粗实线 + `#0D47A1`：双宿主 → 单源能力层。
- 粗实线 + `#0288D1`：能力层 → 外部/观测。
- `endArrow=block;endFill=1`；组内实线 `strokeWidth=1`，跨组主链 `strokeWidth=2`；虚线样式如 `dashed=1`（或 `8 8` 与文件内统一）。

## 画布与分栏

- `grid=1`、`gridSize=10`、`shadow=0`。
- 主图约 **1280×650～700**；**边 32**、**中栏 672**、**双宿主各约 256**；**底栏** 预留「存量 + 图例/图说」高度；内容宽体系约 **1216** 与边距一致。
- 子块坐标在父容器内；父不够高先 **加高父级**。

## 与 v3 template 对齐（主栏界、图说、过渡）

参考 `v3-mdc v2/template.drawio` 的可复用手法，与蓝阶语义叠加：

- **主节点边框标识**：大栏除 **L0～L4 实色底 + strokeWidth=2** 外，可在 **最底图层** 增加整栏 **虚线外框**（`dashed=1;dashPattern=8 8;fillColor=none;strokeColor=#64B5F6`），比内容区大 **8px 边距**，与 template 的「分栏先画虚线再填内容」一致；`pointerEvents=0` 仅当需点穿到底下图元时使用。
- **栏名/图说形式**：**栏名** 保留在分块 **左上角** `align=left;verticalAlign=top;spacingTop=4`；**关键边语义** 用 **edge 自带 label**（`edgeLabel` 或 `value=` 的边上文字），`fontSize=7`～`8`；**阶段/子域短标签** 可用与 template 类似的 **text** 或 **小矩形** 独立摆放，不塞进主链横排。
- **色与 template**：template 以 **无填充虚线** 分栏为主；本规范以 **蓝阶实底** 为主、**虚线外框** 为辅，二选一可并存（外框更强调「区界」），**不**用 template 的默认色块替代 L0～L4。
- **图例/底区**：**图例/读图区** 可用 `dashPattern=12 12` + `#ECEFF1` 底（与 template 大底区虚线步进一致），与 **存量/非目标** 的 `6 4` 或 `8 8` 在步进上区分档。
- **元素间过渡**：**层间/跨栏** 以 **正交实线/虚线** 为主；**`shape=flexArrow`** 仅用于**中间无实色块遮挡**的竖向/横向「总流」表达；分栏内主链**不宜**在密铺卡片下再叠粗箭头，以免被挡或语义重复。

## 出图顺序

1. 层次与四类对象（主/子/备注/图说）  
2. 子节点文案定 min 宽高，同级推齐  
3. 主带尺度与对齐（纵展或横展统一）  
4. 最后叠备注、最后摆底栏图说与色阶图例  
5. 连线并留通道：线不压块、同级不遮挡、多线分轨（与通用 drawio 避让规则一致）

## UI 输出（与图一致）

- 与 draw.io 同一 **蓝阶** + **灰底**；链接/CTA 可用 `#1976D2`。
- CSS 变量示例：`--atx-blue-0`…`--atx-blue-4`、`--atx-legacy`（蓝灰）、`--atx-text`。

## 与方案文档

- 叙事与说法对齐见 **SKILL**；图中落地时**术语、主链、中部能力**与方案一致即可。
